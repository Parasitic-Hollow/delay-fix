# Audio Tools - Dependencias
# ==========================
# Instalar con: pip install -r requirements.txt

# Análisis de metadata de archivos multimedia
pymediainfo>=6.0.0

# Dependencias del sistema requeridas (no Python):
# - ffmpeg (para procesamiento de audio)
# - ffprobe (incluido con ffmpeg)
# - mediainfo (requerido por pymediainfo)
